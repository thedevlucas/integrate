<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recuperar Contraseña - Integrate</title>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="<%= getFile('global/css/styles.css') %>">
    <link rel="stylesheet" href="<%= getFile('global/css/auth.css') %>">
</head>
<body>
    <%- include('header') %>

    <main class="signup-page">
        <div class="signup-container">
            <div class="signup-card">
                <h1>RECUPERAR CONTRASEÑA</h1>
                <p class="subtitle">Ingresa tu correo electrónico para recuperar tu contraseña</p>

                <form class="signup-form">
                    <div class="form-group">
                        <label for="email">CORREO ELECTRÓNICO</label>
                        <div class="input-wrapper">
                            <i class='bx bx-envelope'></i>
                            <input type="email" id="email" name="email" placeholder="Ingresa tu correo electrónico" required>
                        </div>
                    </div>

                    <% if (error) { %>
                        <div class="error-message" style="color: #EF4444; margin-bottom: 1rem; text-align: center;">
                            <%= error %>
                        </div>
                    <% } %>

                    <button type="submit" class="signup-btn">ENVIAR ENLACE</button>

                    <p class="login-link">
                        ¿Recordaste tu contraseña? <a href="/login">Inicia sesión aquí</a>
                    </p>
                </form>
            </div>
        </div>
    </main>

    <%- include('footer') %>
</body>
</html>